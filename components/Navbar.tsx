import { Button } from "@/components/ui/button";

const Navbar = () => {
    return (
        <div>
            <Button>
                Click Me
            </Button>
        </div>
    );
}

export default Navbar;